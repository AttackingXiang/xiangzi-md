# Xiangzi MD 全量质量审查

本文件记录以 `main` 为代码基准、以旧版长图为视觉与交互基准的持续审查。状态只在有代码、测试或实际界面证据时更新。

## 基准

- 代码基准：`main`（当前 CM6 工作分支保留并逐项对照审查）
- 当前分支：`feat/native-code-caret`
- 视觉基准：用户提供的 1868 × 19764 旧版全格式长图
- 完整测试文档：`markdown_all_formats_test.md`
- 优先级：功能正确性与数据安全 > 编辑交互 > 尺寸/布局算法 > 视觉还原 > 代码质量

## 状态说明

- `[ ]` 待检查
- `[~]` 检查中
- `[!]` 已发现问题/修复中
- `[x]` 已修复且已验证
- `[-]` 当前无法验证，必须写明原因

## 模块清单

### 编辑器核心与 Markdown

- `[~]` CM6 文档同步、撤销/重做、选择、输入法与大文档性能
- `[~]` H1–H6、段落、空行、分割线与块间距
- `[~]` 粗体、斜体、删除线、行内代码、链接及隐藏标记删除语义
- `[~]` 无序/有序/混合嵌套列表、任务列表及长列表项对齐
- `[~]` 单层/嵌套引用、GitHub/Obsidian 提示块及引用内复合块
- `[~]` 普通/多语言/无语言/超长代码块与复制、语言切换
- `[~]` 表格渲染、编辑、单元格键盘导航、行高、自适应、横向滚动和性能
- `[~]` 图片路径、加载、复制、缩放、失败态和异步布局
- `[~]` Mermaid 尺寸、自适应、源码切换、复制和失败态
- `[ ]` 数学公式、脚注、HTML、锚点、目录及扩展语法

### 应用功能与工具

- `[ ]` 顶部/底部/上下文/表格/代码块/图片/Mermaid 工具栏
- `[ ]` 查找、替换、全局搜索、大纲与锚点跳转
- `[ ]` 文件树、页签、收藏、标签、最近文件与拖放
- `[ ]` 新建、保存、另存、重命名、移动、删除与未保存提示
- `[ ]` 快捷键、右键菜单、焦点、剪贴板、撤销栈与平台差异
- `[ ]` 阅读/源码/专注/打字机模式、主题、字体、字号、行高和页面宽度
- `[ ]` 设置、更新、帮助、恢复默认值及错误提示

### 导出与安全

- `[~]` 盘点全部导出入口和 Tauri/Pandoc 链路
- `[~]` HTML、PDF、图片/长图、纯文本/Markdown 与富文本复制
- `[~]` 导出中的图片、表格、代码、公式、Mermaid、分页和长文档
- `[~]` 临时 DOM/文件清理、失败恢复、路径与文件名边界
- `[~]` HTML/SVG 注入、外部链接、图片协议与本地文件访问边界

### 场景与回归

- `[~]` 全格式测试文档覆盖度审查与补齐
- `[ ]` 浅色/深色主题、字体/字号/行高/页面宽度组合
- `[ ]` 小窗口/大窗口、高 DPI、缩放、多页签和重新打开
- `[~]` 大文件、大代码块、大表格、大图和多 Mermaid 性能
- `[~]` 前端全量检查、覆盖率、依赖审计、Rust 检查与真实浏览器回归均已通过；原生打包与多平台界面仍待发布流水线验证

## 已确认并处理的问题

- `[x]` 隐藏 Markdown 标记不可见却可被误删；已使用原子范围和边界删除语义保护。
- `[x]` 标题内容删空后残留 `#`/格式外壳；已按删除前 AST 清理并保持撤销语义。
- `[x]` 标题删空后光标落入 0 高度分隔行；当前空行在光标离开前保持可编辑。
- `[x]` CM6 默认标题 token 下划线与旧版不一致；标题内默认下划线已移除。
- `[x]` 文档级段落缺少块节奏、分割线未按 AST 渲染；已恢复段落首末行间距及 HorizontalRule widget。
- `[x]` 分割线承载行错误继承正文行高；已将承载行压为 0 行高，仅保留 `0.24em` 上下间距，宽度保持正文 100%。
- `[x]` 单回车软换行与双回车新段落在输入前后间距跳变；空白编辑行现在预先获得其最终段落边界装饰，并同步相邻段落边界，输入前后布局保持稳定。
- `[x]` 代码块末行回车后视觉上裂出第二个空代码框；源码并未新增围栏，已修正 fenced body 排他尾边界的末行计算，使新空行继续属于原卡片。
- `[x]` 浅色主题代码块背景过灰；编辑区代码卡片改为代码色与页面背景混合，保留边框辨识且不连带改变表格背景。
- `[x]` 列表仍显示源码 `-`/缩进而非所见即所得标记；已按 AST 渲染无序、有序、嵌套和任务列表前缀，并把完整源码前缀设为原子范围。
- `[x]` 引用标记后的源码空格残留、嵌套引用只有一层边线且边界删除无响应；已隐藏完整引用前缀、按实际层级绘制边线，并提供逐级退格/可见字符删除语义。
- `[x]` Setext 标题的 `===`/`---` 源码行隐藏字符后仍占一条小行；已支持 Setext H1/H2，并将结构标记行折叠、从上下键导航中跳过，删空标题时同步清理标记。
- `[x]` 1 MiB 性能页仍引用已删除的旧编辑器且被 TypeScript/ESLint 排除；已迁移到真实 CM6 核心并重新纳入静态检查。
- `[x]` 每个可见代码块预先挂载 140+ 个语言选项；改为首次打开选择器时按需构建，实测同一大文档视口 DOM 从 794 降至 365。
- `[x]` 表格单元格尾部的首个 Shift+Enter 在 WebKit 中没有稳定行盒；现在插入不写入源码的零宽光标哨兵，首次即可显示并编辑 `<br>` 换行。
- `[x]` 表格上下键在单元格边界退化为浏览器横向/不可预测移动；现在仅在单元格首/末行纵向切换同列单元格，表格顶/底边界回到文档而非左右跳格。
- `[x]` 大纲整行都被拖放手势接管，普通点击可能被拖放状态干扰；现在仅拖拽把手可重排，标题文本点击始终用于跳转。
- `[x]` GitHub/Obsidian 风格 `[!NOTE]`、`[!TIP]`、`[!WARNING]`、`[!IMPORTANT]` 提示块未作为提示块呈现；已在可见视口内以安全的 Markdown quote 装饰和标签渲染，源文本仍可编辑。
- `[x]` 普通段落点击被标题的源码标记补偿逻辑重新定位到行首；已只对标题使用该补偿，段落与行内样式回归 CM6 原生精确命中。
- `[x]` CM6 把每条源码空行一律压为不可选分隔行，连续换行被吞掉；现在按连续空行串分类：仅首行作为块分隔，额外空行保留为可选、可编辑的显式留白。
- `[x]` 普通正文 Enter 仍是源码软换行，与所见即所得块模型不一致；现在顶层正文/标题 Enter 拆分 Markdown 块并写入 `\n\n`，Shift+Enter 写入可移植 `\\\n` 硬换行且隐藏其标记。
- `[x]` 在显式空白行直接删除会让 CM6 光标落进折叠的结构分隔行；现在将其视为 ProseMirror 空段落，退格/删除一次移除整个空段落并把光标送到可见相邻块。
- `[x]` 列表/引用仍按源码换行处理，导致 Enter、Shift+Enter、首尾 Backspace/Delete 的块边界不一致；现在统一为块拆分、续写、退出和相邻同类块合并事务。
- `[x]` 大纲与页内锚点各自推导标题位置/重复 slug；现在共用完整源码标题索引，虚拟化时标题未挂载也可稳定跳转。
- `[x]` 代码块删空后空行被压扁，光标落到隐藏关闭围栏；现在保留 CM6 空行盒并修复选区删除的空代码行，部分删除围栏会被拦截，避免相邻代码块误合并。
- `[x]` 普通浏览器直接打开 Vite 页面只显示标题栏并连续触发 Tauri IPC 错误；现在自动切换到内存预览适配层，可打开示例工作区、示例 Markdown 和设置页，并明确显示“浏览器预览模式”。
- `[x]` 代码块内全选依赖后台语法树恰好完成，前端全量并行测试中会偶发退化为选择整篇文档；现在执行命令前确保语法树至少解析到光标位置。

## 验证记录

- 2026-07-13：附件与旧版长图已完整读取并分段核对。
- 2026-07-13：开始 Mermaid、表格、导出三个专项并行审查。
- 2026-07-13：Live Preview 标题/段落/分割线/列表/引用/Setext 专项 42 项测试通过，TypeScript 与定向 ESLint 通过。
- 2026-07-13：真实浏览器 783,668 字符/49,831 行性能页：首个双 RAF 19.9 ms，中部事务 4.6 ms，跳至文末 28.8 ms；仅挂载 54 行、365 个 DOM 节点。
- 2026-07-13：真实 CM6 页面将文档缩减为“标题 / 正文 / 分割线 / 尾段”后目视回归：分割线保持正文 100% 宽度且承载行未再继承正文行高；标题左边界 Backspace 正常变为段落，Delete 只删除首个可见字符，标题删空后可直接继续输入，未残留 `#` 或跳入零高结构行。
- 2026-07-14：正文回车节奏、列表/引用块事务、代码块末行回车/删除围栏保护、提示块、表格坐标式垂直导航与 ProseMirror 式块分隔/空段落删除新增回归场景；前端全量 62 个测试文件、489 项测试通过，ESLint、TypeScript、Vite production build 与 `git diff --check` 通过。
- 2026-07-15：浏览器预览实际打开首页、示例文档和设置页；粗体/斜体/删除线、任务列表、提示块、表格、代码块、KaTeX 公式及 Mermaid 均成功渲染。前端全量 66 个测试文件、529 项测试通过，格式、ESLint、TypeScript、Knip 与 Vite production build 通过；Rust 格式、Clippy 及 55 项测试通过。
- 2026-07-16：前端全量 68 个测试文件、576 项测试、Vitest 覆盖率门禁、Knip、npm audit、bundle 预算与 Rust 55 项测试全部通过；3 项 Playwright 真实浏览器回归覆盖代码块光标、选区及横向滚动。删除未使用的表格放大链路和多余 capability，并把代码块语言匹配及几何定位从主模块拆出。RustSec 审计对当前 macOS/Windows 发布目标无阻断漏洞；Linux-only `wayland-scanner` 的已知传递依赖例外已显式记录，恢复 Linux 发布前必须升级。

## 暂时无法验证

- `[-]` Windows/Linux 原生菜单、路径与窗口行为：当前执行环境为 macOS；先以单元测试和平台适配代码审查覆盖，最终保留真实平台验证项。
- `[-]` 所有系统打印/Pandoc 外部安装组合：需根据本机可用依赖逐项验证，缺失依赖场景必须验证错误提示。
