# macOS 验收记录（2026-06-27）

环境：Apple Silicon、macOS、Tauri release bundle，窗口 1200 × 800。

## 已通过

- 冷启动进入原版简洁编辑器界面；旧版最近文件、最近文件夹、收藏与会话可恢复。
- 原生文件夹选择器可打开 `xiangzi_md/sample-notes`，侧栏显示 `欢迎.md`。
- 文件树支持按目录懒加载；打开不同文件会形成标签页并切换活动文件。
- Milkdown/Crepe 所见即所得编辑器可渲染真实长文档。
- 源码模式与所见即所得模式可双向切换，切换过程未写入磁盘。
- 跨文件全文搜索在实际知识库中返回文件数、匹配数、行号和高亮摘要。
- 中文原生菜单包含文件、编辑、视图和窗口操作，菜单快捷键会转发到 React 命令层。
- 第二次从命令行带入 Markdown 文件时保持单进程，并在现有窗口追加为活动标签。
- 干净窗口可正常退出；存在脏标签时显示 Tauri 原生警告框，选择“取消”后窗口和未保存内容均保留。
- release 构建成功；`.app` 约 13 MB，`.dmg` 约 6.4 MB。
- 导出渲染核心在本地浏览器语料上通过：PNG 为 `920 × 6066`、有效 PNG 文件头、约 679 KB；JPEG 为有效 JPEG 文件头、约 998 KB；PDF 为有效 `%PDF-` 文件头、5 页、约 3.1 MB。
- 加入按需加载的导出依赖后重新完成 release 构建；`.app` 约 14 MB，`.dmg` 约 6.7 MB。

## 本轮发现并修复

- `freezePrototype: true` 会让 Milkdown/ProseMirror 初始化失败并出现空白画布；已关闭该选项，保留 CSP、命令白名单和运行时路径授权。
- 关闭流程中的 WebView `window.confirm` 在 macOS 上不稳定；已改为 Tauri 原生警告框并增加防重入。

## 仍待验收

- 写操作使用隔离副本验证：保存、另存为、自动保存、新建、重命名、移动、回收站、附件粘贴。
- Mermaid、KaTeX、自定义 CSS、主题、标题编号、大纲拖拽与查找替换专项语料。
- Finder 双击文件关联（安装 DMG 后）与中英文菜单切换回归。
- 解锁桌面后的 HTML、PDF、PNG/JPG 保存对话框端到端回归；本轮锁屏状态下已完成渲染核心、格式和分页验证。
- PDF 当前是分页位图；可选择文字、可点击链接和 Electron 打印基线保真仍需 macOS/Windows 原生适配器。
- Mermaid、KaTeX、内嵌图片、深浅主题与 20000px 极限长图的专项导出视觉基线。
- Windows x64 全量清单。
