# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
src-tauri/target
*.local
*.tsbuildinfo
vite.config.js
vite.config.d.ts
vitest.config.js
vitest.config.d.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local visual-audit captures can contain workspace paths or document text.
/audit/current-state/*.png
/audit/final-state/*.png
# Ad-hoc QA screenshots taken at the repo root during design reviews.
/qa-*.png

# Local Playwright CLI sessions and snapshots.
/.playwright-cli/
/.playwright-mcp/
/test-results/
/playwright-report/
/coverage/

# Local ad-hoc verification output (screenshots, captures) not meant to be versioned.
/output/

# Local doc assets that are not meant to be versioned.
/docs/assets/

# Internal engineering notes (migration plans, QA logs, size baselines) — kept
# locally for reference but not meant for the public repo alongside user docs.
/docs/internal/

# Draft for an unrelated product concept, kept locally for reference only —
# not part of Xiangzi MD and must never be committed to this repo.
/docs/AI_PROJECT_MEMORY_PRODUCT_CONCEPT.md
